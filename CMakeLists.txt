PROJECT(Sauron)

CMAKE_MINIMUM_REQUIRED(VERSION 3.9)

SET(SAURON_MAJOR_VERSION 0)
SET(SAURON_MINOR_VERSION 1)
SET(SAURON_PATCH_VERSION 0)
SET(SAURON_VERSION ${SAURON_MAJOR_VERSION}.${SAURON_MINOR_VERSION}.${SAURON_PATCH_VERSION})

SET(SAURON_ROOT_DIR "${CMAKE_CURRENT_SOURCE_DIR}")

INCLUDE(${SAURON_ROOT_DIR}/cmake/Common.cmake)
INCLUDE(${SAURON_ROOT_DIR}/cmake/Platform.cmake)
INCLUDE(${SAURON_ROOT_DIR}/cmake/Compiler.cmake)

SET_PROPERTY(GLOBAL PROPERTY USE_FOLDERS ON)
